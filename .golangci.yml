run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - revive
    - gocritic
    - gosec

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
